# docker-compose.yml
version: '3.8'

services:
  flask-api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    networks:
      - flask-network

networks:
  flask-network:
    driver: bridge
